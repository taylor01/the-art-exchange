# frozen_string_literal: true

# Anthropic API configuration for Claude Vision integration
# The anthropic gem doesn't require explicit configuration
# API key is passed directly to the client when needed
